# Push Review Fixes

## Ticket: {{ ticketId }}

**{{ title }}**

## Working Directory

You are working in: `{{ worktree }}`
Branch: `{{ branch }}`
Repository: `{{ repo }}`

## Instructions

1. Stage all modified files using `git add`.
2. Create a commit with a message like: "Address review feedback for {{ ticketId }}".
3. Push the changes to the remote branch.
4. Do not create a new PR — the existing PR will be updated automatically.
