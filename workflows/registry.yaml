workflows:
  - name: minimal
    file: minimal.yaml
    description: "Minimal workflow with a single script phase"
    tags:
      - test
      - minimal

  - name: standard
    file: standard.yaml
    description: "Standard workflow: setup, implement, self-review, simplify, verify, create PR, review cycle, merge, cleanup"
    tags:
      - standard
      - agent

  - name: bugfix
    file: bugfix.yaml
    description: "Bugfix workflow: minimal-change fix, verify, create PR, review cycle, merge, cleanup"
    tags:
      - bugfix
      - agent
